import 'package:flutter/cupertino.dart';

class NavItemPage extends StatelessWidget {
  const NavItemPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
            "Nav Item Page"
        ),
      ),
    );
  }
}
